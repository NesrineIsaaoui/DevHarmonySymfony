{% extends 'baseBack.html.twig' %}

{% block title %}Edit Publication{% endblock %}

{% block body %}
<div class="container py-4">
    <h1 class="display-4 mb-4">Edit Publication</h1>
    
    <div class="d-flex justify-content-between">
        {{ include('publication_back/_delete_form.html.twig', {'attr': {'class': 'd-inline'}}) }}
    </div>
    <a href="{{ path('app_publication_back_index') }}" class="btn btn-secondary">Back to List</a>
    <div class="card border-0 shadow mb-4">
        <div class="card-body">
            {{ include('publication_back/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    
</div>
{% endblock %}
