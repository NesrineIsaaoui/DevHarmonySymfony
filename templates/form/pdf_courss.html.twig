<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Formation</title>
    <style>
        .card {
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 20px;
            padding: 10px;
            background-color: #f9f9f9;
        }
        .card-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .card-text {
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Liste des Formation</h1>
        {% for cours in Cours %}
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Nom: {{ cours.coursname }}</h5>
                <p class="card-text">Description: {{ cours.coursdescription }}</p>
                <p class="card-text">Prix: {{ cours.coursprix }}</p>
                {% set imagePath = 'uploads/images/' ~ cours.coursimage %}
                {% set absoluteImagePath = app.request.schemeAndHttpHost ~ asset(imagePath) %}
                <img src="{{ absoluteImagePath }}" style="width:80px; height:80px;" />
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>
