{% extends 'admin.html.twig' %}

{% block body %}
<div class="content-page">
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Liste des reservation</h5>

                            <!-- Formulaire de recherche -->
                            <form action="{{ path('afficher_categoriecodepromo') }}" method="GET">
                                <div class="form-group">
                                    <label for="search">Rechercher par  code :</label>
                                    <input type="text" class="form-control" id="search" value="search" value="{{ app.request.query.get('search') }}">
                                </div>
                                <button type="submit" class="btn btn-primary">Rechercher</button>
                            </form>
                            <!-- Fin du formulaire de recherche -->

                            <!-- Tableau des categoriecodepromos filtrées -->
                            {% for recl in Categoriecodepromo %}
                            <div class="card mb-3">
                                <div class="card-body">
                                    <h5 class="card-title">value: {{ recl.value }}</h5>
                                    
                                    
                                    
                                    <p class="card-text">Code: {{ recl.code }}</p>
                                    
                                    
                                    
                                    
                                    <div class="btn-group" role="group" aria-label="Actions">
                                        
                                        <a class="btn cur-p btn-danger" href="{{ path('delete_av', {'id': recl.id}) }}">Supprimer</a>
                                        <a class="btn cur-p btn-success" href="{{ path('update_av', {'id': recl.id}) }}">mise a jour</a>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                            <!-- Fin du tableau des categoriecodepromos filtrées -->

                            <!-- Pagination -->
                            <div class="pagination">
                                {% set currentPage = Categoriecodepromo.currentPageNumber %}
                                {% set previous = currentPage > 1 ? currentPage - 1 : false %}
                                {% set next = currentPage < Categoriecodepromo.pageCount ? currentPage + 1 : false %}
                                <ul class="pagination">
                                    {% if previous %}
                                        <li class="page-item">
                                            <a class="page-link" href="{{ path('afficher_categoriecodepromo') }}?page={{ previous }}">Précédent</a>
                                        </li>
                                    {% endif %}
                                    <li class="page-item active">
                                        <span class="page-link">{{ currentPage }}</span>
                                    </li>
                                    {% if next %}
                                        <li class="page-item">
                                            <a class="page-link" href="{{ path('afficher_categoriecodepromo') }}?page={{ next }}">Suivant</a>
                                        </li>
                                    {% endif %}
                                </ul>
                            </div>
                            <!-- Fin de la pagination -->
                            <!-- Ajoutez ce lien à la suite de votre contenu -->
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
