{% extends 'base.html.twig' %}

{% block body %}

<style>
    h1{
        color: #D75404;
        font-weight: 100;
        font-size: 40px;
        margin: 40px 0px 20px;
    }

    #clockdiv{
        font-family: sans-serif;
        color: #fff;
        display: inline-block;
        font-weight: 100;
        text-align: center;
        font-size: 30px;
        margin-left:  65%;  
    }

    #clockdiv > div{
        padding: 10px;
        border-radius: 3px;
        background: #f2935a;
        display: inline-block;
    }

    #clockdiv div > span{
        padding: 15px;
        border-radius: 3px;
        background: #D75404;
        display: inline-block;
    }

    .smalltext{
        padding-top: 5px;
        font-size: 16px;
    }
</style>
    <h1 style="margin-top: 150px"></h1>
 
     {{  include('quiz_and_test_front/show_timer_test.html.twig') }}
    <h1  class="text-center"> Test : {{ test.sujet }}</h1>
    <form action="{{ path('validate_test',{'id':test.id}) }}" method="post" id="myForm">
        {% set i = 1 %}
        {% for question in test.questions %}
            {{  include('quiz_and_test_front/_form_questionnaire.html.twig') }}
            {% set i = i + 1 %}
        {% endfor %}
        <input type="text" name="user" value="{{ app.session.get('user_id') }}" hidden>
        <h4 id="error" style= "color:red"> </h4>
        <button onclick="validerTest();" type="button" style="width: 25%;margin: auto;margin-top: 50px;" class="btn btn-dark btn-lg btn-block">Valider</button>
    </form>
  
   
  
{% endblock %}
