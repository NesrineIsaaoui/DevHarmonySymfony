<form method="post" action="{{ path('app_categoriecodepromo_delete', {'id': categoriecodepromo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoriecodepromo.id) }}">
    <button class="btn btn-outline-danger btn-lg">Delete</button>
</form>
