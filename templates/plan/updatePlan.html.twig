{% extends 'base.html.twig' %}
{% block body %}
<br><br>
<div class="row">
    <div class="col-lg-8 offset-lg-2">
    <h2 class="contact-title">Add a new Plan</h2>
        {{ form_start(f, {'attr': {'class': 'form-contact contact_form', 'id': 'contactForm', 'novalidate': 'novalidate'}}) }}
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        {{ form_widget(f.nom, {'attr': {'class': 'form-control', 'placeholder': 'Enter Task Name'}}) }}
                        {{ form_errors(f.nom, {'attr': {'class': 'form-control', 'placeholder': 'Enter Task Name'}}) }}
                    </div>
                </div>
            </div>
            <div class="form-group mt-3">
                {{ form_widget(f.Submit, {'attr': {'class': 'button button-contactForm boxed-btn'}}) }}
            </div>
        {{ form_end(f) }}
    </div>
</div>
{% endblock %}