{{ form_start(form, {'attr': {'novalidate': 'novalidate'}, 'class': 'form-horizontal'}) }}
<div class="form-group row">
    <label class="col-sm-2 col-form-label">Content</label>
    <div class="col-sm-10">
        {{ form_widget(form.contenu, {'attr': {'class': 'form-control', 'placeholder': 'Enter content'}}) }}
    </div>
</div>

<div class="form-group row">
    <label class="col-sm-2 col-form-label">Date of Comment</label>
    <div class="col-sm-10">
        {{ form_widget(form.dateCommentaire, {'attr': {'class': 'form-control', 'placeholder': 'YYYY-MM-DD'}}) }}
    </div>
</div>

<div class="form-group row">
    <label class="col-sm-2 col-form-label">Publication</label>
    <div class="col-sm-10">
        {{ form_widget(form.publication, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group row">
    <label class="col-sm-2 col-form-label">User</label>
    <div class="col-sm-10">
        {{ form_widget(form.utilisateur, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group row">
    <div class="col-sm-10 offset-sm-2">
        <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
</div>

{{ form_end(form) }}
