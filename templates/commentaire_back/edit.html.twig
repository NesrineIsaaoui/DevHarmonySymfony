{% extends 'baseBack.html.twig' %}

{% block title %}Edit Commentaire{% endblock %}

{% block body %}
<div class="container py-4">
    <h1 class="display-4 mb-4">Edit Commentaire</h1>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_commentaire_back_index') }}" class="btn btn-outline-secondary">Back to List</a>

    </div>
    <div class="card border-0 shadow mb-4">
        <div class="card-body">
            {{ include('commentaire_back/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

        {{ include('commentaire_back/_delete_form.html.twig') }}
</div>
{% endblock %}
